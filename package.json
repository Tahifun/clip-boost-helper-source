{
  "name": "clip-boost-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "obs-agent.mjs",
  "scripts": {
    "start": "node obs-agent.mjs",

    "build:bundle": "esbuild obs-agent.mjs --bundle --platform=node --target=node18 --format=cjs --outfile=dist/obs-agent.bundle.cjs",

    "build:win": "npm run build:bundle && pkg dist/obs-agent.bundle.cjs --targets node18-win-x64 --output dist/CLiP-BOOsT-Helper.exe",

    "build:linux": "npm run build:bundle && pkg dist/obs-agent.bundle.cjs --targets node18-linux-x64 --output dist/CLiP-BOOsT-Helper-linux-x64",

    "build:mac": "npm run build:bundle && pkg dist/obs-agent.bundle.cjs --targets node18-macos-x64 --output dist/CLiP-BOOsT-Helper-mac-x64 && pkg dist/obs-agent.bundle.cjs --targets node18-macos-arm64 --output dist/CLiP-BOOsT-Helper-mac-arm64"
  },
  "dependencies": {
    "obs-websocket-js": "^5.0.7",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "pkg": "^5.8.1"
  }
}
